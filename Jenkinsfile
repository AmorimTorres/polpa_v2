pipeline {
    agent any
    stages {
        stage('Checkout') {
            steps {
                echo ""
                echo "Starting checkout of the Polpa V2 repository..."
                echo ""
                checkout scm
            }
        }
    }
}